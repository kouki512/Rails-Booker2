<div class="container px-5 px-sm-0">
  <div class="errors">
  <% if @new_book.errors.any? %>
    <h3><%= @new_book.errors.count %>errors prohibited this obj from being saved:</h3>
    <p><% @new_book.errors.full_messages.each do |message| %></p>
    <ul>
    <li><%= message %></li>
    </ul>
      
    <% end %>
  <% end %>
  </div>
  <div class="row">
    
<%= render 'users/user_info', user: @user, new_book: @new_book %>

    <div class="col-md8 offset-md-1">
      <h2>Books</h2>
      <table class="table table-hover table-inverse">
        <thead>
          <tr>
            <th></th>
            <th>Title</th>
            <th>Opinion</th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <% @books.each do |book| %>
          <tr>
            <td>
              <a href="#"><%= attachment_image_tag @user, :profile_image, :fill, 40, 40, format: 'jpeg', fallback: "no_image-icon.jpg" %></a>
            </td>
            <td>
              <%= link_to book_path(book.id) do %>
                <%= book.title %>
              <% end %>
            </td>
            <td><%= book.body %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>